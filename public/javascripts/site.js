function siteInit( ) {
	// initialize the login form
	initLoginForm( "hbloginF" );

	// fix up the menu for IE6/7
	//fixMenuIE( "menu" );

	// Set the CU name for the leaveConfirmation dialog
	window.cuName = "AutoClub Federal Credit Union";
}